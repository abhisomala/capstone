# Core data pipeline (used by src/data and tests — all runnable today)
requests>=2.31
pandas>=2.0
numpy>=1.24
pyyaml>=6.0
scikit-learn>=1.3

# Testing
pytest>=7.4

# Modeling (satellite-imagery model — not yet wired end-to-end; see README)
# torch>=2.1
# torchvision>=0.16
